# docker
DOCKER_USER=kdu0312
DOCKER_REPOSITORY=ai-health
# HTTPS 운영 기본 설정입니다. 최초 Let's Encrypt 발급 전에는 ../nginx/prod_http.conf로 임시 전환할 수 있습니다.
NGINX_CONF=../nginx/prod_https.conf

# app/runtime
# 운영자가 반드시 교체해야 하는 값: SECRET_KEY, COOKIE_DOMAIN, CORS_ALLOW_ORIGINS, FRONTEND_BASE_URL.
APP_VERSION=v1.0.0
ENV=prod
SECRET_KEY=PLEASE_CHANGE_ME
COOKIE_DOMAIN=your-api-domain.example
REFRESH_TOKEN_COOKIE_NAME=refresh_token
REFRESH_TOKEN_COOKIE_PATH=/api/v1/auth
REFRESH_TOKEN_COOKIE_SAMESITE=lax
REFRESH_TOKEN_COOKIE_SECURE=true
CORS_ALLOW_ORIGINS=https://your-frontend-domain.example
LOGIN_FAILURE_LIMIT=5
LOGIN_SOFT_LOCK_MINUTES=1

# email / SMTP
# 운영 메일 발송을 켜려면 EMAIL_ENABLED=true와 SMTP_* 값을 배포 환경에 설정하세요.
# 실제 secret 값은 운영 비밀관리 도구나 배포 환경변수에만 설정하세요.
EMAIL_ENABLED=true
EMAIL_VERIFICATION_DEBUG=false
PASSWORD_RESET_DEBUG=false
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
SMTP_FROM_NAME=AI HealthCare
SMTP_USE_TLS=true
FRONTEND_BASE_URL=https://your-frontend-domain.example

# storage / S3
# 부트캠프 데모 운영 기준은 EC2 + S3입니다.
# 업로드 파일은 private S3 bucket에 저장하고, EC2에는 S3 접근 권한을 가진 IAM Role을 붙이는 것을 권장합니다.
# AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY는 이 파일에 넣지 마세요.
# S3_BUCKET_NAME은 운영자가 실제 private bucket 이름으로 채워야 합니다.
STORAGE_BACKEND=s3
LOCAL_STORAGE_ROOT=var/storage
S3_BUCKET_NAME=
S3_REGION=ap-northeast-2
S3_PREFIX=ai-health/
S3_PRESIGNED_URL_EXPIRES_SECONDS=3600

# ai-worker
AI_WORKER_VERSION=v1.0.0

# frontend
FRONTEND_VERSION=v1.0.0
# Vite loads frontend public env from the repository root. Keep real values in deployment env/secrets.
VITE_API_BASE_URL=/api/v1

# LLM / OpenAI
# 실제 secret 값은 운영 비밀관리 도구나 GitHub Actions Secrets에만 설정하세요.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
CHATBOT_USE_REAL_LLM=false
RAG_RETRIEVAL_STRATEGY=keyword_only
MAIN_CHATBOT_RAG_STRATEGY=keyword_only
RAG_ENABLED=true
ANALYSIS_EXPLANATION_LLM_REWRITE_ENABLED=false
DIET_RECOMMENDATION_LLM_REWRITE_ENABLED=false
DIET_RECOMMENDATION_RAG_STRATEGY=keyword_only
RAG_EMBEDDING_ENABLED=false
RAG_EMBEDDING_PROVIDER=disabled
RAG_EMBEDDING_MODEL=text-embedding-3-small
RAG_EMBEDDING_DIMENSION=1536
RAG_EMBEDDING_BATCH_SIZE=64

# AI provider flags
# 운영에서 실제 provider를 켤 때는 secret, 비용 제한, 관측 설정이 준비된 뒤 true로 전환하세요.
DIET_GPT_VISION_ENABLED=false
DIET_GPT_VISION_MODEL=gpt-4o
DIET_VISION_PROVIDER=rule_based

# Public Data API - MFDS Food Nutrition DB
# 운영에서 식약처 영양성분 매칭을 켤 때 true로 전환하고 service key를 배포 secret에 설정하세요.
# 실제 service key는 이 example 파일에 넣지 않습니다.
DIET_MFDS_ENABLED=false
MFDS_SERVICE_KEY=
MFDS_SERVICE_KEY_ENCODED=
DIET_MFDS_TIMEOUT_SECONDS=5.0
DIET_MFDS_MAX_CANDIDATES=5

EXAM_OCR_PROVIDER=fallback
EXAM_GPT_VISION_ENABLED=false
EXAM_GPT_VISION_MODEL=gpt-4o
PADDLE_OCR_ENABLED=false

# observability / Langfuse
# Langfuse is used for trace/prompt/eval observability, not as a RAG engine.
# Do not put real keys in example env files.
# Cloud example: LANGFUSE_BASE_URL=https://jp.cloud.langfuse.com
# Self-host example: set this to the reachable self-host URL for the runtime.
# Cloud keys and self-host keys are not interchangeable.
LANGFUSE_ENABLED=false
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_BASE_URL=https://jp.cloud.langfuse.com

GPT_VISION_FALLBACK_ENABLED=false

# PostgreSQL
# 이번 데모 운영 범위에서는 RDS를 사용하지 않고 EC2 내부 postgres 컨테이너를 사용합니다.
DB_HOST=postgres
DB_PORT=5432
DB_USER=ozcoding
DB_PASSWORD=PLEASE_CHANGE_ME
DB_NAME=ai_health
DB_POOL_MIN_SIZE=1
DB_POOL_MAX_SIZE=5
DB_COMMAND_TIMEOUT=60

# Redis
# 이번 데모 운영 범위에서는 ElastiCache를 사용하지 않고 EC2 내부 redis 컨테이너를 사용합니다.
REDIS_HOST=redis
REDIS_PORT=6379

# scheduler
# 데모 운영에서 자동 알림 발송 루프를 켤 때 true로 전환하세요.
SCHEDULER_ENABLED=false
SCHEDULER_INTERVAL_SECONDS=60
